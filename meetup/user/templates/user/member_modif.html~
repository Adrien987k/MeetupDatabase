<h2> Here, tou can modify your profile </h2>

<form action="{% url 'member_modification' user_id %}" method="post">
{% csrf_token %}
    <input type="text" name="name" id="id" value={{ name }} />
    <label for="name">Name</label><br />
    <input type="text" name="bio" id ="bio" value={{ bio }} />
    <label for="bio">Name</label><br />
    {% for city in cities %}
    <input type="radio" name="city_name" id="city{{ forloop.counter }}" value="{{ city.city_name }}"/>
    <label for="rating{{ forloop.counter }}">{{ city.city_name }}</label><br />
{% endfor %}
<input type="submit" value="Save" />
</form>
