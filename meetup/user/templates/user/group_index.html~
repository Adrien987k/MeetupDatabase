{% if group_list %}
    <h1>Group list where {{ filt }} = {{ filt_pk }}</h1>
    <ul>
    {% for group in group_list %}
        <li><a href="{% url 'group_detail' group.group_id%}">{{ group.group_name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No group here.</p>
{% endif %}
