{% if member_list %}
  <h1>Member list (page {{ page }}</h1>
  {% if filt != "user"%} <h2> where {{ filt }} = {{ filt_name }}</h2> {% endif %}
    <ul>
    {% for member in member_list %}
      <li><a href="{% url 'member_detail' member.member_id%}">{{ member.member_name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>Sorry, no member here.</p>
{% endif %}
